const Sequelize = require('sequelize');

// nome do banco, usuário, senha, opções com {}
const sequelize = new Sequelize('crud', 'root', '', {
    dialect: 'mysql',
    host: 'localhost',
    port: 3306
})

module.exports = sequelize;